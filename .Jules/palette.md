## 2024-05-23 - Tooltips for Task Defaults
**Learning:** Users often don't know where default values come from in Creator plugins.
**Action:** When a field (like resolution) defaults to a value from the Task/Asset context, always add a tooltip explicitly stating "Default is from task settings" to prevent confusion about whether the value is arbitrary or data-driven.
